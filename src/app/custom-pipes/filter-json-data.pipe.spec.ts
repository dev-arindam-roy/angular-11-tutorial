import { FilterJsonDataPipe } from './filter-json-data.pipe';

describe('FilterJsonDataPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterJsonDataPipe();
    expect(pipe).toBeTruthy();
  });
});
