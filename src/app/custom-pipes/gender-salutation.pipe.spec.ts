import { GenderSalutationPipe } from './gender-salutation.pipe';

describe('GenderSalutationPipe', () => {
  it('create an instance', () => {
    const pipe = new GenderSalutationPipe();
    expect(pipe).toBeTruthy();
  });
});
