import { HighlightTextDynamicEventDirective } from './highlight-text-dynamic-event.directive';

describe('HighlightTextDynamicEventDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightTextDynamicEventDirective();
    expect(directive).toBeTruthy();
  });
});
