import { HighlightTextEventOneDirective } from './highlight-text-event-one.directive';

describe('HighlightTextEventOneDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightTextEventOneDirective();
    expect(directive).toBeTruthy();
  });
});
