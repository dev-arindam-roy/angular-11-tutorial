import { HighlightTextEventTwoDirective } from './highlight-text-event-two.directive';

describe('HighlightTextEventTwoDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightTextEventTwoDirective();
    expect(directive).toBeTruthy();
  });
});
