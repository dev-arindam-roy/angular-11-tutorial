import { HighlightTextOneDirective } from './highlight-text-one.directive';

describe('HighlightTextOneDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightTextOneDirective();
    expect(directive).toBeTruthy();
  });
});
