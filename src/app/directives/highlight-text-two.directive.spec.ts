import { HighlightTextTwoDirective } from './highlight-text-two.directive';

describe('HighlightTextTwoDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightTextTwoDirective();
    expect(directive).toBeTruthy();
  });
});
